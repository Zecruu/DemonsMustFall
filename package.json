{
  "name": "demons-must-fall",
  "version": "0.1.0",
  "private": true,
  "description": "Demons Must Fall — a TypeScript action/combat game scaffold.",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^8.2.2"
  }
}
